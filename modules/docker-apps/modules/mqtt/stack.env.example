BASE_DOMAIN=example.domain

# Credential mqtt-exporter itself uses to subscribe to Mosquitto.
# Add a matching entry to /mnt/data/mqtt/passwd (see README) with the same username/password.
MQTT_EXPORTER_USERNAME=mqtt-exporter
MQTT_EXPORTER_PASSWORD=<mqtt-exporter-broker-password>
